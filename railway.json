{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "npm run backend", "<-- CHANGE THIS LINE"
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "env": {
      "LIBRECHAT_CONFIG_PATH": "/app/librechat.yaml"
    }
  }
}
